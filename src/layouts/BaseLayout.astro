---
interface Props {
  title: string;
  description: string;
  canonical: string;
  schema?: object | object[];
}

const { title, description, canonical, schema } = Astro.props;

const COOKIEYES_ID = import.meta.env.PUBLIC_COOKIEYES_ID || "";
const ADSENSE_ID = import.meta.env.PUBLIC_ADSENSE_ID || "";
const schemaItems = schema ? (Array.isArray(schema) ? schema : [schema]) : [];
const currentPath = new URL(canonical).pathname;
---

<html lang="de">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />

    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag("consent", "default", {
        ad_storage: "denied",
        ad_user_data: "denied",
        ad_personalization: "denied",
        analytics_storage: "denied",
        wait_for_update: 500,
      });
      gtag("set", "ads_data_redaction", true);
      gtag("set", "url_passthrough", true);
    </script>

    {
      COOKIEYES_ID ? (
        <script
          id="cookieyes"
          type="text/javascript"
          src={`https://cdn-cookieyes.com/client_data/${COOKIEYES_ID}/script.js`}
        />
      ) : null
    }

    {
      ADSENSE_ID ? (
        <script
          async
          src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${ADSENSE_ID}`}
          crossorigin="anonymous"
        />
      ) : null
    }

    {
      schemaItems.map((schemaItem) => (
        <script type="application/ld+json" set:html={JSON.stringify(schemaItem)} />
      ))
    }

    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="de_DE" />
    <meta property="og:site_name" content="Einschulungsrechner" />

    <slot name="head" />
  </head>

  <body>
    <header class="base-header">
      <a class="base-header-brand" href="/" aria-label="Zur Startseite von Einschulungsrechner.de">
        Einschulungsrechner.de
      </a>

      <nav class="base-header-nav" aria-label="Hauptnavigation">
        <a class="base-header-link" href="/#einschulungsrechner">Rechner</a>
        <a class="base-header-link" href="/einschulung/">Ratgeber</a>
        <a class="base-header-link" href="/einschulung-checkliste/">Checkliste</a>
        <a class="base-header-link" href="/einschulung/#bundeslaender">Bundesländer</a>
        <a class="base-header-link" href="/ueber-uns/">Über uns</a>
      </nav>
    </header>

    <slot />

    <button type="button" class="base-scroll-top" aria-label="Nach oben scrollen" data-scroll-top>
      ↑
    </button>

    <footer class="base-footer">
      <p class="base-footer-copyright">
        © 2026 DigiTech4U Institute
      </p>

      <nav class="base-footer-nav" aria-label="Rechtliches">
        {currentPath !== "/impressum/" ? <a class="base-footer-link" href="/impressum/">Impressum</a> : null}
        {currentPath !== "/datenschutz/" ? <a class="base-footer-link" href="/datenschutz/">Datenschutz</a> : null}
        {COOKIEYES_ID ? (
          <button type="button" class="base-footer-button" data-cookieyes-settings>
            Cookie-Einstellungen
          </button>
        ) : null}
      </nav>
    </footer>

    <script>
      document.addEventListener("click", (event) => {
        const cookieyesTarget = event.target instanceof Element
          ? event.target.closest("[data-cookieyes-settings]")
          : null;

        if (cookieyesTarget) {
          event.preventDefault();
          if (typeof window.showCookieConsent === "function") {
            window.showCookieConsent();
          } else if (typeof window.CookieYes === "object" && typeof window.CookieYes.show === "function") {
            window.CookieYes.show();
          }
          return;
        }

        const scrollTopTarget = event.target instanceof Element
          ? event.target.closest("[data-scroll-top]")
          : null;

        if (scrollTopTarget) {
          event.preventDefault();
          window.scrollTo({ top: 0, behavior: "smooth" });
          return;
        }

        const backTarget = event.target instanceof Element
          ? event.target.closest("[data-history-back]")
          : null;

        if (!backTarget) return;

        if (window.history.length > 1) {
          event.preventDefault();
          window.history.back();
        }
      });

      let lastScrollY = window.scrollY;
      let ticking = false;

      const updateHeaderVisibility = () => {
        const header = document.querySelector(".base-header");
        const scrollTopButton = document.querySelector(".base-scroll-top");
        if (!header) return;

        const currentScrollY = window.scrollY;
        const scrollDelta = currentScrollY - lastScrollY;

        if (scrollTopButton) {
          scrollTopButton.classList.toggle("base-scroll-top-visible", currentScrollY > 520);
        }

        if (currentScrollY < 80) {
          header.classList.remove("base-header-hidden");
        } else if (scrollDelta > 8) {
          header.classList.add("base-header-hidden");
        } else if (scrollDelta < -8) {
          header.classList.remove("base-header-hidden");
        }

        lastScrollY = currentScrollY;
        ticking = false;
      };

      window.addEventListener(
        "scroll",
        () => {
          if (!ticking) {
            window.requestAnimationFrame(updateHeaderVisibility);
            ticking = true;
          }
        },
        { passive: true }
      );
    </script>
  </body>
</html>

<style is:global>
  .base-header {
    position: sticky !important;
    top: 0 !important;
    z-index: 1000 !important;
    display: flex !important;
    justify-content: space-between !important;
    align-items: center !important;
    gap: 28px !important;
    width: min(960px, calc(100% - 40px)) !important;
    box-sizing: border-box !important;
    margin: 0 auto !important;
    padding: 14px 22px !important;
    border: 1px solid rgba(129, 97, 38, 0.12) !important;
    border-top: 0 !important;
    border-radius: 0 0 22px 22px !important;
    background:
      linear-gradient(180deg, rgba(255, 252, 246, 0.98) 0%, rgba(247, 238, 222, 0.94) 100%) !important;
    box-shadow:
      0 20px 42px rgba(42, 35, 21, 0.14),
      0 4px 12px rgba(129, 97, 38, 0.12) !important;
    backdrop-filter: blur(14px) !important;
    transform: translateY(0) !important;
    transition: transform 0.22s ease !important;
  }

  .base-header.base-header-hidden {
    transform: translateY(-120%) !important;
  }

  .base-header-brand {
    flex: 0 0 auto !important;
    color: #816126 !important;
    font: inherit;
    font-size: clamp(0.88rem, 2.2vw, 1.02rem);
    font-weight: 850;
    letter-spacing: -0.015em;
    line-height: 1.15;
    text-decoration: none;
    white-space: nowrap !important;
  }

  .base-header-brand:hover {
    color: #172033 !important;
  }

  .base-header-nav {
    display: flex !important;
    flex: 0 1 auto !important;
    flex-wrap: nowrap !important;
    justify-content: flex-end !important;
    align-items: center !important;
    gap: clamp(14px, 2.8vw, 30px) !important;
    width: auto !important;
    max-width: calc(100vw - 20px) !important;
    margin: 0 !important;
    white-space: nowrap !important;
  }

  .base-header-link {
    display: inline-flex !important;
    flex: 0 0 auto !important;
    align-items: center !important;
    justify-content: center !important;
    color: #816126 !important;
    font: inherit;
    font-size: clamp(0.72rem, 2vw, 0.88rem);
    font-weight: 650;
    letter-spacing: 0.002em;
    line-height: 1.15;
    text-align: center;
    text-decoration: none;
    white-space: nowrap !important;
  }

  .base-header-link:hover {
    color: #172033 !important;
    text-decoration: underline;
  }

  .base-scroll-top {
    position: fixed !important;
    right: max(16px, env(safe-area-inset-right)) !important;
    bottom: max(18px, env(safe-area-inset-bottom)) !important;
    z-index: 980 !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    width: 42px !important;
    height: 42px !important;
    border: 1px solid rgba(129, 97, 38, 0.16) !important;
    border-radius: 999px !important;
    background: rgba(255, 252, 246, 0.94) !important;
    box-shadow: 0 12px 26px rgba(42, 35, 21, 0.12) !important;
    color: #816126 !important;
    font: inherit !important;
    font-size: 1.15rem !important;
    font-weight: 900 !important;
    line-height: 1 !important;
    cursor: pointer !important;
    opacity: 0 !important;
    pointer-events: none !important;
    transform: translateY(12px) !important;
    transition:
      opacity 0.18s ease,
      transform 0.18s ease,
      color 0.18s ease,
      background 0.18s ease !important;
    backdrop-filter: blur(12px) !important;
  }

  .base-scroll-top.base-scroll-top-visible {
    opacity: 1 !important;
    pointer-events: auto !important;
    transform: translateY(0) !important;
  }

  .base-scroll-top:hover {
    background: #fff8ec !important;
    color: #172033 !important;
  }

  :target {
    scroll-margin-top: 96px;
  }

  .base-footer {
    display: flex !important;
    justify-content: space-between !important;
    align-items: center !important;
    gap: 18px !important;
    width: min(960px, calc(100% - 40px)) !important;
    box-sizing: border-box !important;
    margin: 34px auto 0 !important;
    padding: 16px 0 18px !important;
    border-top: 1px solid rgba(42, 35, 21, 0.07) !important;
    text-align: center !important;
  }

  .base-footer-copyright {
    flex: 0 0 auto !important;
    margin: 0 !important;
    color: rgba(75, 85, 99, 0.78) !important;
    font: inherit;
    font-size: clamp(0.66rem, 2.2vw, 0.78rem);
    font-weight: 500;
    letter-spacing: 0.002em;
    line-height: 1.15;
    white-space: nowrap !important;
  }

  .base-footer-nav {
    display: flex !important;
    flex: 0 1 auto !important;
    flex-wrap: nowrap !important;
    justify-content: flex-end !important;
    align-items: center !important;
    gap: clamp(10px, 2.4vw, 22px) !important;
    width: auto !important;
    max-width: calc(100vw - 20px) !important;
    margin: 0 !important;
    white-space: nowrap !important;
  }

  .base-footer-link,
  .base-footer-button {
    display: inline-flex !important;
    flex: 0 0 auto !important;
    align-items: center !important;
    justify-content: center !important;
    color: rgba(75, 85, 99, 0.82);
    font: inherit;
    font-size: clamp(0.66rem, 2.2vw, 0.78rem);
    font-weight: 550;
    letter-spacing: 0.002em;
    line-height: 1.15;
    text-align: center;
    text-decoration: none;
    white-space: nowrap !important;
  }

  .base-footer-button {
    width: auto !important;
    height: auto !important;
    min-width: 0 !important;
    min-height: 0 !important;
    padding: 0 !important;
    border: 0 !important;
    border-radius: 0 !important;
    background: transparent !important;
    box-shadow: none !important;
    color: rgba(75, 85, 99, 0.82) !important;
    cursor: pointer;
    appearance: none;
  }

  .base-footer-link:hover,
  .base-footer-button:hover {
    color: #172033;
    text-decoration: underline;
  }

  @media (max-width: 520px) {
    .base-header {
      flex-direction: column !important;
      justify-content: center !important;
      align-items: center !important;
      gap: 10px !important;
      width: calc(100% - 28px) !important;
      padding: 12px 16px 11px !important;
      border-radius: 0 0 18px 18px !important;
      text-align: center !important;
    }

    .base-header-nav {
      display: flex !important;
      justify-content: center !important;
      align-items: center !important;
      flex-wrap: nowrap !important;
      gap: clamp(8px, 2.35vw, 13px) !important;
      width: 100% !important;
      max-width: calc(100vw - 36px) !important;
      margin: 0 auto !important;
      white-space: nowrap !important;
    }

    .base-header-link {
      font-size: clamp(0.58rem, 2.45vw, 0.72rem);
      font-weight: 650;
      letter-spacing: -0.012em;
    }
    .base-footer {
      flex-direction: column !important;
      justify-content: center !important;
      gap: 10px !important;
      width: calc(100% - 28px) !important;
      margin-top: 28px !important;
      padding: 14px 0 !important;
    }

    .base-footer-nav {
      justify-content: center !important;
      gap: clamp(7px, 2vw, 10px) !important;
      width: auto !important;
      max-width: calc(100vw - 12px) !important;
      margin: 0 auto !important;
    }

    .base-footer-copyright,
    .base-footer-link,
    .base-footer-button {
      font-size: clamp(0.66rem, 2.75vw, 0.78rem);
      letter-spacing: 0;
    }

    .base-scroll-top {
      right: max(14px, env(safe-area-inset-right)) !important;
      bottom: max(14px, env(safe-area-inset-bottom)) !important;
      width: 38px !important;
      height: 38px !important;
      font-size: 1rem !important;
    }
  }
</style>